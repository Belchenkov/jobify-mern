import {
    Tooltip,
    Bar,
    XAxis,
    YAxis,
    CartesianGrid,
    BarChart as BrChart,
    ResponsiveContainer,
} from 'recharts';

const BarChart = () => {
    return (
        <ResponsiveContainer width="100%" height={300}>
            <h2>Bar Chart</h2>
        </ResponsiveContainer>
    );
};

export default BarChart;
